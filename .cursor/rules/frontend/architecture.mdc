---
name: frontend-architecture
description: Feature-based frontend architecture defining pages, components, services, state management, and strict unidirectional data flow
alwaysApply: true
---

# Frontend Architecture Rules

## 1. Architectural Style
Frontend must follow a feature-based, component-driven architecture.

---

## 2. Layer Responsibilities

### Pages (Route Layer)
- Compose components and features
- Must not contain business logic
- Must not call APIs directly

---

### Components (UI Layer)
- Responsible only for UI rendering
- Must be reusable and single-purpose
- Must not fetch or mutate data directly

---

### Services (Data Layer)
- Handle all API communication
- Provide normalized data to the application
- Must not contain UI logic

---

### State Layer
- Manage feature or domain state
- Separate UI state from business state when applicable

---

## 3. Data Flow Rule
Strict unidirectional flow:

API → Service → State → Component → UI

---

## 4. Separation of Concerns Rule
Each layer has a single responsibility:

- UI logic stays in components
- Data access stays in services
- State orchestration stays in state layer or hooks
- No layer may bypass another

---

## 5. Dependency Rule
- UI depends only on services/state
- Services depend on API clients/adapters
- UI must never depend on raw API structures